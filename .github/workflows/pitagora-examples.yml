name: Pitagora Examples

on:
  push:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions: {}

jobs:
  run-examples:
    runs-on: [self-hosted, pitagora]
    steps:
      - name: Say hello and print hostname
        run: |
          echo "hello"
          hostname
